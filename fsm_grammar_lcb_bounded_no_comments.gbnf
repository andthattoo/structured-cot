# Richer LiveCodeBench plan with no comments/backticks.
# This targets the two observed displacement channels:
#   1. Python-comment scratchpads after </think>
#   2. raw markdown/prose/code drafts after </think>
#
# Note: this was originally a bounded-line grammar, but the deployed
# llama.cpp GBNF parser rejects {m,n} repetition ranges. Keep rules parse-safe.
#
# Each rule MUST be on a single line for llama.cpp's GBNF parser.

root ::= think code
think ::= "<think>\n" "GOAL: " line "STATE: " line "ALGO: " line "EDGE: " line "VERIFY: " line "</think>\n\n"
line ::= [^\n]+ "\n"
code ::= [\x09\x0A\x0D\x20-\x22\x24-\x5F\x61-\x7E]+
